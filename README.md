# Chicken
A demonstration of the hawk-dove game for NISER Open Day 2019. Right now, it
supports only a single player mode: the human player can play against the CPU.

### Running your own instance
Right now, all the code is client side, so you just have to setup a server
(Apache, nginx), to host the files.
